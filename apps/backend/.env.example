# PostgreSQL connection. Add ?sslmode=require for hosted DBs (Neon, Supabase, etc.)
# or ?sslmode=disable for local Postgres without SSL. Silences SSL behavior warning.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Google Sign-In (same Client ID as frontend; used to verify ID tokens)
GOOGLE_CLIENT_ID=""